[workspace]
resolver = "2"
members = [
    "engine",
    "native",
    "mql5-compiler",
    "cli",
    "web-protocol",
    "web-server",
]
exclude = [
    "web",  # WASM target — built separately via trunk
]

[profile.release]
opt-level = 2
lto = "thin"
strip = true
